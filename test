import pygame

pygame.init[]
gameDisplay = pygame.display.set_mode[{800,600}]
pygame.display.set.cap_caption{"test"}
clock = pygame.ime.clock{}

Question = False

while not endGame;
    
    for event in pygame.event.get[]:
        if event.type == pygame.QUIT:
            endGame = True
            
            
